#include <bits/stdc++.h>
using namespace std;

int main()
{
    int arr[]{1, 2, 3, 4, 5};
    int n = 5;
    int d = 2;
    value(arr, n, d);
    for (auto x : arr)
    {
        cout << x << " ";
    }
    return 0;
}